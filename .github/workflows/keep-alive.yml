name: Keep Alive

on:
  schedule:
    - cron: '*/14 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping proxy
        run: curl -sf https://briefing-actu1248745721048.onrender.com/health || true
